-- Run this after Prisma migrations if you need to manually seed compatibility or run functions
-- The main seed is in prisma/seed.ts

-- Example: Mark overdue invoices (run via cron)
-- SELECT mark_overdue_invoices();

-- Example: Run allocation (via API or manually)
-- SELECT run_allocation('2024-25');
